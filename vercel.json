{
  "rewrites": [
    { "source": "/fd-api/:path*", "destination": "/api/fd-api/:path*" },
    { "source": "/((?!api/).*)", "destination": "/" }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite"
}
